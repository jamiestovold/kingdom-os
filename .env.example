# Kingdom Core Environment Variables
# Copy to .env and fill in values — NEVER commit .env

# API authentication token — generate with: openssl rand -hex 32
KINGDOM_API_TOKEN=your_token_here

# Kingdom version
KINGDOM_VERSION=1.0.0
